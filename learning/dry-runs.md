# Forgetting-curve dry runs (test mode)

One section per `/new-entries` batch while the learner profile is in **test mode** (CLAUDE.md §8c):
what `LEARNER.md` said was due, where it **would** have gone in the batch, and which items the batch
reused anyway. Nothing here changed an entry. Read these side by side with the entries to decide
whether to merge the forgetting curve into the writing flow.
